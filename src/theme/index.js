import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    background: {
      default: "#f8f4fc",
      paper: "#fff",
    },
    primary: {
      main: "#071e4e",
    },
  },
});

export default theme;
